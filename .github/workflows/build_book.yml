    - name: Build Beta Reader HTML into docs/
      run: |
        mkdir -p docs
        pandoc \
          book_01_GhostPAT/manuscript/book_01_master.md \
          book_01_GhostPAT/manuscript/ch*.md \
          -f markdown -t html5 \
          -s -o docs/index.html \
          --toc \
          --css beta.css \
          --metadata title="GhostPAT — Beta Reader Draft"

    - name: Copy beta reader CSS into docs
      run: |
        cp book_01_GhostPAT/styles/beta_reader.css docs/beta.css
